[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "8000"
PYTHON_VERSION = "3.11"

[nixpacks]
phases.setup.nixPkgs = ["python311", "postgresql_16.dev", "gcc"]
